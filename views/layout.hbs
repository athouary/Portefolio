<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>{{htmlWebpackPlugin.options.title}}</title>
    \{{#if envIsProduction}}
        <link type="text/css" href="{{htmlWebpackPlugin.files.css}}" rel="stylesheet">
    \{{/if}}
</head>
<body class="cyan-bg">
    \{{>header/header}}
    \{{>home/home}}
    \{{>skills/skills}}
    \{{>about/about}}
    \{{>work/work}}
    \{{>contact/contact}}
    \{{>footer/footer}}
    \{{#if envIsProduction}}
        <script type="application/javascript" src="{{htmlWebpackPlugin.files.chunks.main.entry}}"></script>
    \{{else}}
        <script type="application/javascript" src="assets/scripts/main.js"></script>
    \{{/if}}
</body>
</html>
